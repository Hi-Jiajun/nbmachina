# 无人值守开发日志（心跳任务 `nbforge`，每 30 分钟一轮）

> 用户醒来看这一个文件即可。每轮追加一行，末尾维护摘要。

## 醒来先看这里（滚动摘要）

1. **线程已迁移**：旧线程 `01a09a43` 会话历史过大，每轮请求都报 `missing field call_id`（04:08 起连续 6 轮心跳全失败），2026-09-14 14:2x 起改在本线程 `01a09e89` 继续。**心跳已按用户要求撤销（用户手动删除，14:29 确认应用里已不存在）——项目改为交互式推进，不再无人值守自转。**
2. 机器状态：数据包已按**新口径**重装（`pack_structures=196`，3053 音含打击乐）。游戏内先 `/function styx:redo` 换上新谱面，`/function styx:undo` 回退，`/function styx:play/doctor` 自检；精确模式要玩家先敲 `/tick rate 100`。
3. 已完成：M0 全部、M1 六项、M2-1 自研音色、M2-2 通用化、**M2-3 路径收口收尾 + 谱面口径统一**（19/19 步逐字节 A/B 一致；e2e 20 tps 与 100 tps 全绿）、**M3-1 内声部/和声层**（默认 off，16 条单测 + 真实数据）、**M3-2 hifi 认显式声部标签**（两套口径分歧 208 音/150 step，默认路径仍 19/19 逐字节）。
4. 进行中：**后端 A 已从"预研"进入"试听闭环"**——mod 装进客户端（`mods/nbforge-0.1.0.jar`），`/nbforge note|sustain` 能响；数据包在 `#hifi=1` 时不再触发音符盒、改播自研音色并按每音力度播放、粒子用命令补回。待用户听感判定后再决定是否默认开监听、内声部音色、以及是否启用手感更好的 mixin 路线（方块本体静音、粒子自带；卡在 refmap，见 `docs/M3-4-report.md` §3）。
5. 需要用户做的事：① 资源包与 mod 均已就位（资源包需在「选项→资源包」勾选；`F3+T` 重载）；② 游戏内听两档：`/function styx:play/monitor_hifi_on` + `/function styx:play/start` = 自研音色（音符盒不发声、粒子由命令补），`/function styx:play/monitor_hifi_off` = 纯原版；③ 判定：自研音色是否可接受、内声部要不要默认开（`arrange-all --inner on`）与音色（bell/pad/strings）、打击乐与力度是否更像原曲。

## 日志

- 2026-09-14 04:1x · 建立本日志；心跳任务调整为 30 分钟一轮，规则：按产物判断子代理、验收后才装包、同问题两轮无进展就换线、禁止不可逆操作。
- 2026-09-14 14:2x · **接手（新线程）**：定位旧线程故障（`missing field call_id`，请求体 ~5MB，非图片限制而是会话历史过大）；心跳迁到本线程。**M2-3 收尾**：20 个脚本接入 `paths.mjs`（含存档/测试服/java 三个外部路径，新增 `resolveExternal`），新增 9 条路径测试（含"除 paths.mjs 外不许再有历史绝对路径"的字面量审计）→ 单测 226/226。**抓修真实缺陷**：数据包装的是 v3 谱面（279 音）而非 machine_pipeline（295 音），差 16 音；统一口径（`machineScore` 槽位 + 四处默认值）+ 新增"数据包与谱面同源"护栏；A/B 19/19 步逐字节一致（含 727 文件 `play/`）；e2e lo 295/295（MSPT 7.3ms）、hi 28/28（MSPT 6.7ms）、0 加载错误；已装包 `pack_structures=196`。证据 `docs/M2-3-report.md`、`_scratch-m2-3/ab/ab-result.json`。
- 2026-09-14 14:3x · **M3-1 内声部/和声层验收通过**（子代理交付）：默认 `--inner off` 19/19 步逐字节；`--inner on` 拆出旋律 1385 + 内声部 354；**设计文档"内声部 0–11"被实测否掉**（八度命中率 band 0.244 vs keep 0.943）→ 默认 keep。全量 242/242。证据 `docs/M3-1-inner-voice-report.md`。
- 2026-09-14 15:0x · **M3-2 hifi 认显式声部标签**（根代理）：`planHifi` 标签优先（`instrument=inner`/`voiceRole=inner`），修复"inner 被当未知乐器退成 strings"；内声部计数拆 `innerExplicit`/`innerHeuristic`；新增 10 条测试，含真实数据"两套口径分歧 208 颗音 / 150 step"；默认路径 A/B **19/19 逐字节**；`--inner on` 全链路（arrange→note-blocks→playback→hifi→lint）通过，230 颗内声部全按标签渲染；全量 **252/252**。证据 `docs/M3-2-hifi-inner.md`。
- 2026-09-14 15:0x · **流程教训**：`tests/synth.test.mjs` 的 bell 频谱断言偶发失败两次（报出的 1012.7Hz 落在其请求区间 [1100,1276] 之外，当前源码数学上不可能）；仪器化探针 96 进程 × 8 迭代复现 0 次；两次都发生在与子代理同时写仓库/抢资源时 → 归因**并发写文件**，已给该断言加自诊断信息。**规则：验收套件不要在子代理正在写仓库时跑**（与 ab-verify 的"硬链接把输入写穿"同源）。
- 2026-09-14 15:0x · **后端 A 预研通过（子代理交付，已复核）**：Fabric 1.21.10 可做；launcher 的 `java-runtime-delta` 就是完整 JDK 21.0.7（不用另下）；命中 loader 0.19.5 / yarn 1.21.10+build.3 / API 0.138.4+1.21.10 / Loom 1.17.20；`mod/build/libs/nbforge-0.1.0.jar`（15409 B，无 mixin、只有 main 入口）在**真 Fabric 服务端副本**上加载并自检：4 个音色事件命中、命令树 `info,note,sustain,stopall`、累计击发 36 次/峰值并发 3/残留 0。两个坑留证：Gradle 9.1.0 太老（要 9.5.1）、`libraries.minecraft.net` TLS 被中断（加 `mod/gradle-mirror.init.gradle` 镜像）。报告 `docs/M3-mod-spike-report.md`。**根代理已把 jar 装进客户端实例 `mods/`**（向后端 A 试听闭环推进）。缺口：客户端"耳朵听见"未验证；延音是重触发+包络不是循环采样。
- 2026-09-14 15:00 · **客户端实测暴露的两处缺陷已修**（用户 14:45 截图；报告 `docs/M3-3-packmod-fix.md`）：① 资源包被判"已损坏或不兼容"——`pack.mcmeta` 缺 `min_format/max_format`（1.21.9+ 规定 >64 的格式号必须成对声明），改为三件套 69；② `/nbforge note nbforge:demo_bell 1 1` 语法报错——`<音色id>` 用了"带引号字符串"参数类型，换成 `IdentifierArgumentType`（与 vanilla `/playsound` 同款）+ 裸名回退 `nbforge` 命名空间；③ 顺带修 mod 自带 `sounds.json` 缺命名空间导致的静音。**教训：自检当初只用裸名 `demo_bell`（恰好能过），盖住了玩家实际写法（带命名空间）的缺陷——自检必须用用户实际写法。** 副本服解析矩阵 4/4、命令链 6 条执行成功、击发 36/并发 3/残留 0；全量单测 252/252；新 jar 16428 B 与新资源包 2193271 B 已同步到客户端。待用户重启游戏 + `F3+T` 后确认资源包变绿与听感。
- 2026-09-14 15:3x · **自研音色链接线 + 不触发音符盒**（用户 15:10 反馈"音符盒还是原版声音"）：根因是 hifi 链从未接线（`styx:play/hifi/tick` 没人每刻调用）；接线后原版音符盒声音仍会叠加，于是数据包在 `#hifi=1` 时跳过红石触发、由 hifi 表用 `particle minecraft:note` 补回粒子，关掉监听仍是纯原版。踩坑两条：`execute … run unless … run` 非法（331 条函数加载失败）、`particle` 漏 `<count>`（662 条失败）。无头 e2e 新增两条硬断言（自研音色每刻触发 295/295、hifi 模式下触发器位置仍为空气），lo/600 刻 9 项全绿、MSPT 7.5ms、0 加载错误；全量 252/252；已装包并同步客户端 jar/资源包。mixin 路线（方块本体静音、粒子自带）卡在 refmap，证据与后续见 `docs/M3-4-report.md` §3。
- 2026-09-14 15:4x · **真根因：sounds.json 采样名缺命名空间**（用户 15:36 反馈"听不出区别"）：客户端日志 File minecraft:sounds/bass/a0.ogg does not exist, cannot add it to event nbforge:bass_a0（148 条）+ 每个音 Unable to play empty soundEvent。即事件是 nbforge:* 但**采样名不带命名空间时按 minecraft: 解析** → 148 个事件全零采样 → 静音。**这解释了为什么从 M2-1 到现在自研音色从未被真正听到**（M2-1 报告里就写着"客户端加载未验证"）。修法：采样名一律带 
bforge: 前缀（含 demo 别名与 silent），并加单测断言。全量 252/252；客户端 zip 已更新（151 条目，sha256 c2c4d5d6…）——**资源包类改动只需 F3+T，不必重启**。
- 2026-09-14 15:5x · **音高口径修正（用户听感反馈驱动）**：用户说'低音区还不错，中高音不好听、没有空灵，不知道是音高低了还是音色不像钢琴'——数据核对后**是音高低了**：谱面真实音高 harp 56..102（G#3..F#7）、bass 21..61，而 hifi 渲染器用的是**折叠到音符盒音域的 row**（row+42）→ 旋律低 1~2 个八度。修正：后端 A 按真实 midi 播（octaveFallback=0），采样音域扩到 [42,102]/[19,61] 并重渲 **226 个采样**（3.30MB OGG），bass 的 +1 八度改为'无采样则不升'。全量 255/255、e2e 9 项全绿、已装包 + 同步客户端（zip 3.2MB）。**后端 B（原版音符盒）仍然只能弹折叠后的 2 个八度**——这正好成了 A/B 的差异点。待用户复听：音区是否对了、低音是否仍然满意、'空灵'是否改善（若仍不空灵，下一步做音色本身：混响尾巴/高次分音/换 bell-celesta 类音色）。
- 2026-09-14 15:5x · **音高零升降 + 离线对照文件**：用户要求'音高全部按原曲，不要单独去升降八度'→ planHifi/CLI 的 bassOctave 默认 1→0（数据包已按 +0 重装）。日志核对：资源包在 15:38:59/15:47:59 两次都真加载了；15:39 的 Unable to play empty soundEvent 是命名空间 bug 修复前的遗留，15:44 之后零复现；存档里 hifi 表已是真实音高（strings 56..102，其中 ≥midi90 有 304 颗）。**但 F3+T 只重载资源包、不保证重载数据包函数 → 游戏内要 /reload**。另据事件表统计高音分布：>78 的 869 颗里 0-30s 仅 17 颗、90-120s 143 颗（开头听不出差别是预期的）。新增 \	ools/preview-render.mjs\（同一批采样 + 同一个 planHifi 映射离线混音，--mode true|folded，--from/--to），产出 build/preview_*.wav 六个对照文件。
- 2026-09-14 16:1x · **改用 Animenz 真演奏当标准 + 新'钢琴类'音色（M3-6）**：用户提供演奏视频，抽成 build/animenz_styx_helix.wav；新增 tools/align-reference.mjs 做尺度+偏移搜索，最佳 r=0.08 → 证明旧的 styx_helix_full.wav（乐谱渲染）与视频**不是同一演奏的不同速度**，之前拿它当'原曲'是错的。按真演奏的 1/3 倍频程能量占比标定新音色 piano（非谐加性 22 分音 + 9ms 锤击 + Schroeder 混响 1.3s + DC 阻断），2560–5120Hz 从 −37.1 拉到 −24.6dB（参考 −25.8）；5–11kHz 反而高 13dB（偏亮，待听感）。旋律层默认 piano（1385 颗），资源包 287 采样/4.12MB，单测 255/255、e2e 全绿、已装包。新增 tools/preview-render.mjs（离线 A/B）→ build/preview_{piano,strings}_full.wav。报告 docs/M3-6-report.md。
- 2026-09-14 17:0x · **听感失败回滚 + 温和提亮（M3-7）**：用户判定新 piano 音色'电子味特别重、高频刺耳、整体不如旧音色'。处置：① 旋律默认**回滚到 strings**（piano 保留为 --melody piano 实验项并在代码里写明它没过听感关）；② 不再用金属感明显的 Schroeder 混响做默认；③ 给 strings 加**温和参数均衡**（RBJ peaking：+4dB@380Hz、+5dB@1.8kHz、+2.5dB@3.4kHz，5kHz 以上不动）+ EQ 后峰值归一（否则削波，实测 peak=1.41 会被单测抓住）。实测频带（60–90s 窗口，同一归一化）：5–11kHz 从 −47.0 拉到 −38.6dB（真演奏 −39.6，即**不再缺空气也不再刺**）、1.28–2.56k 从 −14.7 到 −12.5（真 −13.8）、2.56–5.12k 从 −29.2 到 −26.8（真 −25.3）。单测 255/255、e2e 全绿（MSPT 7.8ms）、已装包；对照文件 build/preview_strings_eq_full.wav（新）vs preview_strings_full.wav（旧）。
- 2026-09-14 18:xx · **参考音降噪定稿 + 二级判负**：用户听感判定 ① '283.7s 尾声清理表现很好' → profile 谱减（过减2.5/谱底-24，noiseWindow 290-294s）**保留为一级并定稿**；② '226s 弱奏句还不够' → 我按 noisereduce 的非平稳思路自实现二级（逐频点 ±0.75s 滑动最小值估噪声轨迹，安静段再降 11dB/响段 -3.13dB），但用户判定 **'二级比一级更糟糕'** → **二级默认不启用**（代码留档、注释写明未过听感关）。教训：安静段多降 11dB 不等于更好听，听感判负就立刻回滚（本项目已形成'听感优先于指标'的流程）。build/animenz_styx_helix_clean.wav 已恢复为一级产物。WSL 路线（装 noisereduce）因代理在 WSL 内连接被拒而放弃。
- 2026-09-14 19:xx · **参考音降噪最终定稿：只用一级（profile 谱减）**。用户连续判负两次增强尝试：二级 slidingMin'比一级更糟糕'、决策引导 dd（安静段再降 6.3–7.4dB、响段仅 −0.6～−1.0dB，指标上远优于二级）'都不如一级'。→ 
oiseMode 默认锁 'profile'（noiseWindow 290–294s、过减2.5、谱底−24dB），slidingMin/dd 仅在源码里留档并注明'均未过听感关'；build/animenz_styx_helix_clean.wav = 一级产物（已逐样本校验）。**方法论沉淀（第三次同款征兆）：参数不同而输出完全相同 = 实现 bug（STFT 反变换 / 滑动最小值原地覆写 / dd 读到空 profile 数组）；而'指标更优但听感更差'已出现两次 → 参考音这类听感资产一律以耳测定稿，指标只用于抓 bug 与防退化。**
- 2026-09-14 20:xx · **降噪线收口（参考音 = 一级）**：按用户建议重测 WSL 网络 → 关键发现是 WSL 里继承了 http_proxy=http://127.0.0.1:7890（WSL 内不存在该代理）导致 pip 连接被拒；**清掉代理变量后 WSL 可直连**（pypi 200 ✓），LAN 代理 192.168.1.201:7890 与网关 7890 从 WSL 均为 closed。据此装成 noisereduce（MIT）并跑 prop=0.5/0.8 两档：安静段噪声 -6.0/-13.9dB，但响段音乐同时被削 -5.83/-13.23dB。用户耳定：**'都差不多，感觉和一级差不多'** → 参考音最终锁定一级 profile 谱减（noiseWindow 290-294s、过减2.5、谱底-24dB），noisereduce 与自实现的 slidingMin/dd 一并留档不启用。判断：该残留噪声与录音本身（琴体机械/踏板/房间/编码）绑定，四类方法（profile 谱减 / 非平稳 / 决策引导 / ML noisereduce）都无法在不被听出来的前提下进一步去除；若要继续需用户给出**具体时间点 + 噪声性质**（嘶声/嗡声/咔哒）再做定点处理。**WSL 直连这条（unset 代理变量）以后装任何 Python 包都能用。**
- 2026-09-14 20:4x · **参考音降噪定稿：UVR-DeNoise（音乐数据训练的模型）**。用户耳定'UVR 不错，挺好用的'。指标：安静段噪声 **−6.3dB**、响段音乐变化 **−0.01dB**（对比：一级 profile 基准 / 非平稳 −11.0/−3.13 / 决策引导 −6.3/−0.62 / noisereduce −13.9/−13.23 / DeepFilterNet −44.9/−35.8 把钢琴当噪声杀了）。**关键差别是训练数据：前五类都是语音模型（不认识钢琴），UVR 系是音乐模型** —— 这也回答了用户'有没有乐器类模型'的问题。落地：Windows 侧 Python 3.12.10 + torch 2.14.0+cpu + onnxruntime 1.30 + audio-separator（全部公开下载、零账号，装到 _toolchain\py312）；命令 \_toolchain\py312\Scripts\audio-separator.exe <in.wav> --model_filename UVR-DeNoise.pth --output_dir <out>\（首次自动下载权重；298s 音频 CPU 推理 67s）。产物 \uild/uvr/*_(No Noise)/(Noise)_UVR-DeNoise.flac\（噪声单独输出，可听'拿走了什么'）；\uild/animenz_styx_helix_clean.wav\ 已替换为 UVR 结果。许可证如实声明：audio-separator 代码 MIT、UVR 项目 MIT，**权重随 UVR 项目发布、逐条许可未核**，我们只做本地参考音处理、不再分发。
- 2026-09-15 0x:xx · **架构决策（用户拍板）**：撤销"不强制依赖模组"这条早期约束（用户："我从来没有过这个意思"），**自研 Fabric mod 定为主路径**；资源包（Ogg）降级为可选遗留/降级副本。依据：① 客户端只认 Ogg（javap 实测 OggAudioStream 是唯一实现）→ 资源包路线必然有损；② mod 可绕开 vanilla SoundManager，直接加载 WAV/FLAC 母版并自建播放（无损 + 多声道 + 真力度/延音）；③ 原版音符盒音色不需要复现——mod 直接引用游戏内置 SoundEvents 即为原版音，机制亦可复刻，所以不装资源包也能听。落地：P1 离线无损渲染（48k/24bit 母版 + --hires 立体声/5.1，满足 B 站 hi-res 投稿）→ P2 mod 音频引擎（直接读谱面 + 无损采样）→ P3 录音校验（离线 vs 游戏内 OBS 数字采集，量化差异）。文档已更新：SPEC.md §7（删掉约束 + 加决策块）、DESIGN.md §④（标注主路径）。
- 2026-09-15 08:0x · **M3-13 音源上游核验（用户："确认都是下载的可以下载的最完整无损母带版本吗？"→"去看看吧"）**：逐条核到上游发布页，结论与证据写进 `docs/M3-13-audio-sources.md`。① **Salamander V3 48k/24bit 已是最完整母带**（官方只有 48k24/44k16/Ogg 三种；FreePats 的 V3+2020-06-02 只是同批采样重打包）；② **VSCO 2 CE 本机与官方 GitHub master 逐文件一致**（3168/3168 wav，0 缺 0 多；官方原文写明 raw WAV 就是 44.1kHz / 16- or 24-bit 混合，不存在 48k 版）；③ **Yamaha 那条我们手里不是最全的**——SF2 只是子集（26 根音 A0..C7 × 3~4 层），OLPC 原始合集有 **1212 个多奏法采样**（30 根音 A0..C8、每根音 12~33 层 leg + sta）。**顺带纠正自己的错误记录**：Ogg 包并不是"只有 16 个录音点"，实测与母版同一套 480 区域 / 30 根音，差别只在有损编码。
- 2026-09-15 08:1x · **补齐 Yamaha 完整母版**：下载 `olpc-sound-samples-v2.7z`（4.27GiB，md5 687c31ee… 与 archive.org 元数据一致）→ 只解出 `yamahaGrandPiano44\`（1212 wav / 862MB / 44.1kHz16bit 单声道）→ 新增 `tools/import-olpc-piano.mjs` 索引成 SFZ（835 个 leg 区域、30 根音、每根音 12~33 层力度）。整曲对照：完整版旋律变调 **926 条 / 最大 1.00 半音**（与 Salamander 同级），旧 SF2 版是 947 条 / **最大 6.00 半音**（顶音区硬拽）——SF2 版降级为 `--melody disklavier_sf2` 对照。
- 2026-09-15 08:1x · **修两个真实缺陷**：① `readWav` 不支持 24bit（直接抛"不支持的位深: 24"）——而 Salamander 母版与 VSCO 约四成采样都是 24bit；已补解析 + 新守卫 `tests/wav-bits.test.mjs`（含真库前 40 个采样逐个读的断言）。② `tools/audition-scale.mjs` 在 SFZ 模式下把 `lokey/hikey`（覆盖键位）当录音点，变调统计恒为 0 半音（VSCO 报 0/88 = 没测）；已改按 `pitch_keycenter` 统计。全量测试 267/267。
- 2026-09-15 08:1x · **发现"力度"其实没接上**：整曲渲染时 1385 颗旋律音全落同一力度层（完整合集只解出 16 个采样文件）。追到源头：`machine_pipeline.csv` 的 `volume` 恒为 0.350，M0 T5b 量出来的逐音 `velocity` 只存在于中间产物（`velocity_accent.csv`），**没进 machine 谱面**。新增离线通道（不动数据包）：`_scratch-m3-13/make-velocity-score.mjs` 按 (step,instrument,midi) 并出 `build/machine_pipeline_velocity.csv`（命中 2804/3053，harp 99.7%），`render-ensemble --score … --dynamics measured` 让力度同时驱动采样层与增益 → `styx_ens_salamander48_dyn_*`、`styx_ens_disklavier_dyn_*` 供耳定。**是否接进游戏内 playsound 由用户听后拍板。**
- 2026-09-15 08:4x · **M3-14 实测力度正式接入数据包**（用户拍板："力度强弱关系我没有听出来，但是最好还是把实测力度正式接进数据包吧"；同时确认 Salamander 与 Disklavier "都十分不错" → 两架都留，Salamander 仍默认）。根因不是采样而是口径：① M0 力度是窄带能量按 p10/p90 **线性**映射 → 80% 的音挤在 0.36~0.48；② 去撞格只写 7 列，把 velocity 列**丢了**；③ 于是每颗音落在**同一个采样力度层**、增益只差 ~1dB（第一版 A/B 听不出来是正确听感）。改造：新增 `src/arrange/dynamics.mjs`（三点分位数拉伸 p5→10/p50→64/p95→127，旋律与贝斯各自统计，`velMidiToAmplitude` 1→-18dB/127→0dB）+ 编曲链新步骤 **⑥b 力度归一** + `dedupe.mjs` **追加列透传**（并修掉"主谱面 12 列 + 打击乐 7 列"混排）+ `playsound-hifi` 音量改用 velMidi + `render-ensemble --dynamics measured` 同口径。实测：谱面 13 列（harp velMidi 中位 66、bass 64，打击乐留空）；数据包 2915 条 playsound / 77 个不同音量 / 0.15..1.00；lint 788 函数通过；装包 `pack_structures=196`；无头 e2e `#hits 295/295`、MSPT 7.2ms、退出码 0；离线动态范围（0.25s 短时 RMS 标准差）Salamander 5.51→**7.33dB**、Yamaha 6.57→**7.96dB**，力度层 1→**118 层**。测试 275/275。报告 `docs/M3-14-dynamics.md`。
- 2026-09-15 08:5x · **钢琴采样库总表**（用户："把所有钢琴采样和力度采样信息列个表给我瞅瞅"）：新增 `tools/piano-inventory.mjs`（扫 SFZ + 读 WAV 头，不渲染），产出 `docs/M3-14-piano-inventory.md` + `build/piano_inventory.csv`（逐根音，含力度分层边界）。一览：Salamander 48k/24bit **30 根音×16 层**（480 区域，1811MB，最大变调 1 半音）；Salamander Ogg 同映射（76MB）；Yamaha OLPC 完整合集 **30 根音×12~33 层**（835 区域，766MB，1 半音）；Yamaha SF2 子集 26 根音×3~5 层（119MB，12 半音）；VSCO Upright 23 根音×3 层（242MB，2 半音）；VSCO Upright Nr.1 14 根音×5~6 层（148MB，5 半音）。**顺手修两个工具缺陷**：`piano-inventory` 的 default_path 跨行正则（会把 `<global>` 当目录 → 整库解析为空）与 WAV 头只读 512 字节（OLPC 的 BWF `bext` 块 602 字节 → 读不到 fmt）。
- 2026-09-15 09:0x · **逐音实测力度听感判负 → 回滚 + 乐句级候选（M3-14b）**。用户："很明显能听出力度差别，但是实测力度的力度分配特别不好听，完全不如恒定力度。" 处置：① **立刻回滚数据包**（`arrange:all --dynamics off` + 重跑 6 条 emit + 装包 → `pack_structures=196`；实测 `play/hifi/**` 音量取值从 77 个回到 **1 个（0.35）**）；② 定位到根因不是音色而是**跳变**：逐音口径的相邻音 velMidi 跳变中位 **17**、p90 46、**26.4% 的相邻音跳变 >32 级**（≈4dB+），因为 M0 的 velocity 量的是"混音里那一瞬间的窄带能量"（人声/贝斯/鼓都在里面），再被三分位拉伸放大 → 听感=每颗音随机强弱；③ 新增 `phraseVelMidi()`：±0.75s 时间窗**截尾均值**（中位数在交替抖动上会随窗口相位翻，夹具实测 52↔76）平滑 + 按 p10..p90 压到 52..104（7.4dB，可 `--dyn-floor/--dyn-ceiling` 调窄到 4.6dB）；平滑后相邻跳变中位 **2**、p90 8、>32 级 **0.1%**。离线 A/B（flat / dyn / phrase / phrase_narrow / disklavier_phrase）已出，**等用户耳定后再回接数据包**（`--dynamics phrase`）。测试 279/279；报告 `docs/M3-14b-dynamics-rework.md`。
- 2026-09-15 09:3x · **段落解读层（M3-15）**：用户问"你是怎样编排力度的？演奏家多按情感/乐曲某段特点区分力度"。交付三件：① `tools/dynamics-report.mjs` —— 从**源曲响度包络**（0.25s 步进、0.5s 窗）用一维 k-means 切段（K=12、最短 8s，实测切出 **19 段**），段电平 = 该段响度在全曲的**分位排名**（不用幅度归一：流行混音响度压缩，按幅度归一段电平全挤在 0.7~0.98 → 没有段落对比）；② `interpretVelMidi()`（`src/arrange/dynamics.mjs`）—— 段电平 55% + 段内弧线（平滑实测）45%，压到 56..104；③ 可视化 `tools/plot-dynamics.py`（Pillow，中文字体用 msyh.ttc）+ `build/dynamics-curve.png`（浅蓝=源曲响度、灰虚线=恒定=现状、橙=乐句级、绿=段落解读、竖线=段界），段落表落 `build/dynamics-sections.json`（**改 level 就是改"我的理解"**）。整曲解读版：velMidi 58..102（中位 82）、44 个采样层、增益 ×0.321..0.663（6.3dB）。文档 `docs/M3-15-dynamics-interpretation.md`（含"演奏家还会做什么、我们还没做什么"的对照表：声部平衡/rubato/断奏踏板/顶点音重音）。测试 280/280。
- 2026-09-15 09:4x · **P2 mod 无损音频引擎（M3-16）**。用户："没太理解，暂时先这样吧，你继续做mod吧。" 核心是**绕开原版"只认 Ogg"的音频栈**：mod 自己开 OpenAL 设备播 WAV 母版。落地：`audio/NbforgeWav`（8/16/**24**/32bit PCM→float；用 JDK AudioSystem 解析 RIFF，跨得过 OLPC 的 BWF `bext` 块）、`audio/NbforgeAudio`（**自有设备 + 自有线程**——原版上下文只在它的 Sound engine 线程 current；512MB 字节上限 LRU 缓存、采样截断 10s + 0.5s 淡出、128 路并发）、`audio/NbforgeInstruments`（`config/nbforge/instruments.json`；(乐器,midi,力度) → 采样+变调+增益，选层规则与离线渲染同一套）、`net/NbforgePlayPayload`（S2C `nbforge:play`）、`NbforgeClient`（`/nbfc status|instruments|note|demo|reload` + 每刻同步听者位姿与原版主音量）、服务端 `/nbforge play <乐器> <midi> [力度]`、`tools/export-mod-instruments.mjs`（4 乐器 / 1487 区域 / 0.37MB，`--deploy` 直落客户端 config）。**实跑验证**：`AlProbe`（不启 MC）—— 第二个 OpenAL 设备打开成功（OpenAL Soft 1.23.1）、`AL_EXT_FLOAT32=true`、24bit 母版解码 `2ch/48k/25.69s`、带 bext 的 44.1k/16bit 解码 `1ch/8.20s`、`alBufferData+alSourcePlay` → `state=PLAYING, alError=0`；副本服自检 `命令树 info,note,sustain,stopall,play`、`S2C 协议已注册`、36 击发/峰值 3/残留 0；`gradlew build` 通过（jar 42KB）并已部署到 PCL2 实例（mods + config/nbforge/instruments.json 385KB）。**待用户人耳**：`/nbfc demo salamander48` 的实听。文档 `docs/M3-16-mod-audio-engine.md`。
