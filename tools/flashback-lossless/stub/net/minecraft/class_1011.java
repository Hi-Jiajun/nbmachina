package net.minecraft;

/**
 * Compile-time stub for the only Minecraft class referenced by AsyncFFmpegVideoWriter
 * (com.mojang.blaze3d.platform.NativeImage, intermediary name net.minecraft.class_1011).
 * Members/names are taken verbatim from the released Flashback 0.39.9 jar, so the bytecode
 * javac emits here is identical to the shipped one and needs no yarn/intermediary remap.
 */
public class class_1011 implements AutoCloseable {
    public long field_4988; // NativeImage.pixels
    public long field_4987; // NativeImage.size

    public int method_4307() { // NativeImage.getWidth()
        return 0;
    }

    public int method_4323() { // NativeImage.getHeight()
        return 0;
    }

    @Override
    public void close() {
    }
}
